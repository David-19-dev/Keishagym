# Exercise photos — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/ex/
# 3D demo videos, dropped in by whoever runs the instance
media/vid/

# Runtime data — users, passkeys, per-user state, session secret
data/

# Local config
.env

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store

# Local AI-assistant tooling — contributor's own setup, not part of the app
.claude/
.codex/
CLAUDE.md
AGENTS.md
graphify-out/

# Local Vite env (Supabase URL + anon key per environment)
*.local
