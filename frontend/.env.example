# Supabase backend (accounts + sync). Copy to .env.local and fill in; leave empty to build
# without accounts. The anon key is public by design — never put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
